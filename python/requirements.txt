# Core library dependencies (matches pyproject.toml [project] dependencies).
# CLI: pip install -e ".[cli]"  |  Dev+CLI: pip install -e ".[dev,cli]"
kubernetes>=28.1.0
requests>=2.31.0
pyyaml>=6.0
python-dateutil>=2.8.0
